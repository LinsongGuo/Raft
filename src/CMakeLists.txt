file(GLOB_RECURSE SRC_LIST *.cpp *.cc *.h)
add_executable(${PROJECT_NAME} ${SRC_LIST})
target_link_libraries(${PROJECT_NAME} 
	PRIVATE
	${Boost_LIBRARIES} 
	${PROTOBUF_LIBRARIES} 
	${GRPC_LIBRARIES}
)

TARGET_INCLUDE_DIRECTORIES(${PROJECT_NAME} PRIVATE ${Boost_INCLUDE_DIRS})

